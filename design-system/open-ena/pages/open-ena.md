# Open ENA page override

- The page may use spacious marketing sections, but the workbench preview must preserve the parent ENA information density and its left-rail / controls / visualization hierarchy.
- Keep the five parent modes visible: Sets, Data, Model, Plot, Stats.
- Make the preview boundary explicit: controls may demonstrate UI state only; no analysis output is presented as computed.
- Baby blue is the primary accent. Use navy for the tool rail and all high-contrast text.
- On screens below 900px, change the three-column workbench into a single readable stack; never force a desktop-width horizontal canvas.
- All preview controls remain keyboard reachable and at least 44px tall. Disabled analysis actions must have an adjacent explanation.
